fn main() {
    println!("Hello, world!");
}

struct Solution{}

impl Solution {
    pub fn character_replacement(s: String, k: i32) -> i32 {
        if s.len() <= k + 1 { return s.len() }
        let mut low = 
    }
}
